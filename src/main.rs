use std::io;

fn main()
{
    //2869번
    // let mut input = String::new();
    // io::stdin().read_line(&mut input).expect("Error");
    // let mut input = input.split_ascii_whitespace().flat_map(str::parse::<i32>);
    // let morning:i32 = input.next().expect("Error");
    // let night:i32 = input.next().expect("Error");
    // let wood:i32 = input.next().expect("Error");
    // let mut day_count:i32 = (wood - morning) / (morning - night);
    // if (wood - morning) % (morning - night) != 0
    // {
    //     day_count += 1;
    // }
    // println!("{}", day_count+1);
}